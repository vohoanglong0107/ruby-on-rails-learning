<%=
  link_to gravatar_for(
    current_user,
    size: Settings.user.gravatar.index_size
  ), current_user
%>
<%= content_tag :h1, current_user.name %>
<%= content_tag :span, link_to(t(".view_my_profile"), current_user) %>
<%= content_tag :span, t(".micropost", count: current_user.microposts.count) %>
